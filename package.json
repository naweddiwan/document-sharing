{
  "name": "razorpay-machine-coding",
  "version": "1.0.0",
  "description": "Razorpay maching coding interview",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -r dist",
    "build": "tsc --build --pretty",
    "prestart": "npm run build",
    "start": "node dist/index.js"
  },
  "author": "Nawed Diwan",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^16.11.13",
    "datastructures-js": "^10.1.1"
  }
}
